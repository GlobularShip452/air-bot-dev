const Discord = require('discord.js');

module.exports = {
    name: 'queue',
    description: 'Lets you see the queue of the music playing.',
    guildOnly: true,
    execute(message, args, client) {
        
        

    },
};